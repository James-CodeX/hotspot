TZ=Africa/Nairobi

# Use either DATABASE_URL or the split DB_* variables below.
DATABASE_URL=mysql://avnadmin:REPLACE_ME@phpnuxbill-xx.k.aivencloud.com:26019/defaultdb?ssl-mode=REQUIRED

DB_HOST=phpnuxbill-xx.k.aivencloud.com
DB_PORT=26019
DB_NAME=defaultdb
DB_USER=avnadmin
DB_PASS=REPLACE_ME
DB_SSL_MODE=REQUIRED

# Set this only if you want certificate verification with a mounted CA file.
DB_SSL_CA=

# Optional: set APP_URL only when you must force a public base URL,
# such as behind a reverse proxy or when serving from a subpath.
# APP_URL=https://billing.example.com